*** Begin Patch
*** Update File: apps/autorecorder/pattern_write_dummy.c
@@
 #include "pattern_writer.h"
 #include <string.h>
 #include <stdint.h>
 
 /* A safe, minimal pattern write: copy steps into a local buffer and call optional hook.
  * Real implementation should persist to pattern RAM/flash.
  */
 
 int pattern_write_pattern(int part, const ft_step_t *steps, int count)
 {
-    (void)part; (void)steps; (void)count;
-    return 0;
+    /* Basic checks */
+    if (!steps || count <= 0) return -1;
+
+    /* Weak hook that the platform can provide to persist the pattern */
+    extern int pattern_write_hook(int part, const ft_step_t *steps, int count) __attribute__((weak));
+    if (pattern_write_hook) {
+        return pattern_write_hook(part, steps, count);
+    }
+
+    /* fallback: just accept and do nothing (but return success) */
+    (void)part;
+    (void)count;
+    /* We could memcpy to a RAM cache if desired for local tests. */
+    return 0;
 }
*** End Patch
